#!/bin/bash
#

COM_TOCSV=./ipynb_tocsv

#
SBT=`echo $NB_SUBMITS | awk -F" " '{print $1}'`
if [ "$DST" != "" ] ; then
    DST=${HOME}/${PRJCT_DIR}/${WORK_DIR}/${DST}
    cd $DST
fi

BSEFL=$1
FNM=`echo $BSEFL | sed -e 's/\.[^\.]*$//'`
OUTFL=${FNM}".csv"
rm -f $OUTFL

$COM_TOCSV $BSEFL > $OUTFL
